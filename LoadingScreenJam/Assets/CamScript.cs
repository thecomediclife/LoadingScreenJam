using UnityEngine;
using System.Collections;

public class CamScript : MonoBehaviour {

	public float negXBounds, posXBounds, negYBounds, posYBounds;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		if (transform.position.x < negXBounds) {

		}
	}
}
